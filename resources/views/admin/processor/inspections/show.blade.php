<x-layouts.admin title="Inspection {{ $inspection->batch?->batch_code }} | FishTrace">@include('admin.processor._nav')<a href="{{ route('admin.processor.inspections.index') }}" class="text-sm text-cyan-700">← Inspections</a><h1 class="mt-2 text-3xl font-bold">{{ $inspection->batch?->batch_code }}</h1><p class="text-slate-500">Quality inspection · {{ $inspection->result->value }}</p><section class="mt-6 grid gap-4 md:grid-cols-4"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Grade</p><strong>{{ $inspection->qualityGrade?->code ?? '—' }}</strong><div>{{ $inspection->qualityGrade?->name }}</div></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Product temperature</p><strong>{{ $inspection->product_temperature }} °C</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>pH level</p><strong>{{ $inspection->ph_level ?? '—' }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Inspector</p><strong>{{ $inspection->inspector?->name }}</strong><div>{{ $inspection->inspected_at?->format('Y-m-d H:i') }}</div></article></section><section class="mt-6 rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Sensory findings</h2><dl class="mt-3 grid gap-4 md:grid-cols-2"><div><dt class="text-slate-500">Appearance</dt><dd>{{ $inspection->appearance }}</dd></div><div><dt class="text-slate-500">Odor</dt><dd>{{ $inspection->odor }}</dd></div><div class="md:col-span-2"><dt class="text-slate-500">Notes</dt><dd>{{ $inspection->notes ?? 'No notes recorded.' }}</dd></div></dl></section><section class="mt-6 flex gap-4 rounded-xl bg-white p-6 shadow-sm"><a href="{{ route('admin.batches.show',$inspection->batch) }}" class="text-cyan-700">Open traceability record</a>@if($inspection->processingRecord)<a href="{{ route('admin.processor.records.show',$inspection->processingRecord) }}" class="text-cyan-700">Open processing record</a>@endif</section></x-layouts.admin>
