<x-layouts.admin title="Quality Inspections | FishTrace">@include('admin.processor._nav')<header class="flex items-end justify-between"><div><p class="text-sm font-medium text-cyan-700">Processor operations</p><h1 class="text-3xl font-bold">Quality inspections</h1></div><a href="{{ route('admin.processor.inspections.export',request()->query()) }}" class="rounded-lg bg-cyan-700 px-4 py-2 text-white">Export CSV</a></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Batch code" class="rounded-lg border-slate-300"><select name="result" class="rounded-lg border-slate-300"><option value="">All results</option>@foreach(App\Enums\InspectionResult::cases() as $result)<option value="{{ $result->value }}" @selected(($filters['result']??'')===$result->value)>{{ $result->value }}</option>@endforeach</select><select name="quality_grade_id" class="rounded-lg border-slate-300"><option value="">All grades</option>@foreach($grades as $grade)<option value="{{ $grade->id }}">{{ $grade->code }} · {{ $grade->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Batch</th><th>Processor</th><th>Result / grade</th><th>Temperature / pH</th><th>Inspector</th><th>Inspected</th></tr></thead><tbody>@forelse($inspections as $inspection)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.processor.inspections.show',$inspection) }}" class="font-semibold text-cyan-700">{{ $inspection->batch?->batch_code }}</a></td><td>{{ $inspection->organization?->name }}</td><td>{{ $inspection->result->value }}<div>{{ $inspection->qualityGrade?->code ?? '—' }}</div></td><td>{{ $inspection->product_temperature }} °C / {{ $inspection->ph_level ?? '—' }}</td><td>{{ $inspection->inspector?->name }}</td><td>{{ $inspection->inspected_at?->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No inspections match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $inspections->links() }}</div></section></x-layouts.admin>
