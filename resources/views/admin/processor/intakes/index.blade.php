<x-layouts.admin title="Processor Intakes | FishTrace">@include('admin.processor._nav')<header><p class="text-sm font-medium text-cyan-700">Processor operations</p><h1 class="text-3xl font-bold">Batch intake queue</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Batch code" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option>@foreach(['ACCEPTED','REJECTED'] as $status)<option @selected(($filters['status']??'')===$status)>{{ $status }}</option>@endforeach</select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}" @selected(($filters['organization_id']??'')===$org->id)>{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Batch</th><th>Processor</th><th>Decision</th><th>Weight</th><th>Received</th><th>Processing</th></tr></thead><tbody>@forelse($intakes as $intake)<tr class="border-b"><td class="p-4"><a class="font-semibold text-cyan-700" href="{{ route('admin.processor.intakes.show',$intake) }}">{{ $intake->batch?->batch_code }}</a></td><td>{{ $intake->organization?->name }}</td><td>{{ $intake->status }}</td><td>{{ $intake->received_weight_kg ?? '—' }} kg</td><td>{{ $intake->received_at?->format('Y-m-d H:i') }}<div>{{ $intake->receiver?->name }}</div></td><td>{{ $intake->processingRecord?->status?->value ?? 'Not started' }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No intakes match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $intakes->links() }}</div></section></x-layouts.admin>
