<x-layouts.admin title="Intake {{ $intake->batch?->batch_code }} | FishTrace">@include('admin.processor._nav')<a href="{{ route('admin.processor.intakes.index') }}" class="text-sm text-cyan-700">← Intakes</a><h1 class="mt-2 text-3xl font-bold">{{ $intake->batch?->batch_code }}</h1><p class="text-slate-500">Batch intake · {{ $intake->status }}</p><section class="mt-6 grid gap-4 md:grid-cols-3"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Processor</p><strong>{{ $intake->organization?->name }}</strong><div>{{ $intake->receiver?->name }}</div></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Received</p><strong>{{ $intake->received_weight_kg ?? '—' }} kg</strong><div>{{ $intake->received_at?->format('Y-m-d H:i') }}</div></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Source batch</p><a href="{{ route('admin.batches.show',$intake->batch) }}" class="font-bold text-cyan-700">Trace batch</a><div>{{ $intake->batch?->species?->common_name }}</div></article></section>
@if($intake->rejection_reason || $intake->notes)<section class="mt-6 rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Decision notes</h2><p class="mt-2">{{ $intake->rejection_reason }}</p><p>{{ $intake->notes }}</p></section>@endif
@if($intake->processingRecord)<section class="mt-6 rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Processing</h2><a href="{{ route('admin.processor.records.show',$intake->processingRecord) }}" class="mt-2 inline-block text-cyan-700">Open {{ $intake->processingRecord->processingType?->name ?? 'processing record' }} →</a></section>@endif
</x-layouts.admin>
