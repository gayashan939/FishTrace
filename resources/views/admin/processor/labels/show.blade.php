<x-layouts.admin title="{{ $label->label_code }} | FishTrace">@include('admin.processor._nav')<a href="{{ route('admin.processor.labels.index') }}" class="text-sm text-cyan-700">← Package labels</a><h1 class="mt-2 text-3xl font-bold">{{ $label->label_code }}</h1><p class="text-slate-500">Privacy-safe package label record</p><section class="mt-6 grid gap-4 md:grid-cols-4"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Batch</p><a href="{{ route('admin.batches.show',$label->batch) }}" class="font-bold text-cyan-700">{{ $label->batch?->batch_code }}</a></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Package count</p><strong>{{ $label->package_count }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Weight per package</p><strong>{{ $label->package_weight_kg }} kg</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Total labelled weight</p><strong>{{ number_format((float)$label->package_weight_kg*$label->package_count,3) }} kg</strong></article></section><div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Origin</h2><p class="mt-3">Processor: {{ $label->batch?->organization?->name }}</p><p>Product: {{ $label->batch?->product_type }}</p>@foreach($label->batch?->parentLinks ?? [] as $link)<p>Parent batch: <a href="{{ route('admin.batches.show',$link->parent) }}" class="text-cyan-700">{{ $link->parent?->batch_code }}</a></p>@endforeach</section><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Retail custody</h2>@if($label->retailReceipt)<p class="mt-3">Received by {{ $label->retailReceipt->retailerOrganization?->name }}</p><p>Location: {{ $label->retailReceipt->location?->name }}</p><p>Received: {{ $label->retailReceipt->received_at?->format('Y-m-d H:i') }}</p><p>Inventory: {{ $label->retailReceipt->inventoryLot?->status?->value ?? 'Not created' }}</p>@else<p class="mt-3">No retailer receipt recorded.</p>@endif</section></div></x-layouts.admin>
