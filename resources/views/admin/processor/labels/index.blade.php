<x-layouts.admin title="Package Labels | FishTrace">@include('admin.processor._nav')<header><p class="text-sm font-medium text-cyan-700">Processor operations</p><h1 class="text-3xl font-bold">Package labels</h1><p class="mt-1 text-slate-500">Operational label metadata only; public trace tokens are never displayed.</p></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Label or batch code" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All processor organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}" @selected(($filters['organization_id']??'')===$org->id)>{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Label</th><th>Batch / product</th><th>Processor</th><th>Packages</th><th>Printed</th><th>Retail custody</th></tr></thead><tbody>@forelse($labels as $label)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.processor.labels.show',$label) }}" class="font-semibold text-cyan-700">{{ $label->label_code }}</a></td><td>{{ $label->batch?->batch_code }}<div>{{ $label->batch?->product_type }}</div></td><td>{{ $label->batch?->organization?->name }}</td><td>{{ $label->package_count }} × {{ $label->package_weight_kg }} kg</td><td>{{ $label->printed_at?->format('Y-m-d H:i') ?? 'Not recorded' }}</td><td>{{ $label->retailReceipt?'RECEIVED':'AWAITING RECEIPT' }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No labels match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $labels->links() }}</div></section></x-layouts.admin>
