<x-layouts.admin title="Processor Facilities | FishTrace">@include('admin.processor._nav')<header><p class="text-sm font-medium text-cyan-700">Processor operations</p><h1 class="text-3xl font-bold">Facilities</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Facility, licence, or operator" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}" @selected(($filters['organization_id']??'')===$org->id)>{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Facility</th><th>Organization</th><th>Operator</th><th>Licence</th><th>Status</th></tr></thead><tbody>@forelse($facilities as $facility)<tr class="border-b"><td class="p-4"><a class="font-semibold text-cyan-700" href="{{ route('admin.processor.facilities.show',$facility) }}">{{ $facility->facility_name }}</a></td><td>{{ $facility->organization?->name }}</td><td>{{ $facility->user?->name }}<div class="text-slate-500">{{ $facility->user?->email }}</div></td><td>{{ $facility->license_number ?? '—' }}</td><td>{{ $facility->organization?->is_active?'ACTIVE':'INACTIVE' }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center">No facilities match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $facilities->links() }}</div></section></x-layouts.admin>
