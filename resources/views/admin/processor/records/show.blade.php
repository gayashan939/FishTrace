<x-layouts.admin title="Processing {{ $record->batch?->batch_code }} | FishTrace">@include('admin.processor._nav')<a href="{{ route('admin.processor.records.index') }}" class="text-sm text-cyan-700">← Processing records</a><h1 class="mt-2 text-3xl font-bold">{{ $record->batch?->batch_code }}</h1><p class="text-slate-500">{{ $record->processingType?->name ?? 'Processing' }} · {{ $record->organization?->name }}</p><section class="mt-6 grid gap-4 md:grid-cols-4"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Status</p><strong>{{ $record->status->value }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Input</p><strong>{{ $record->input_weight_kg }} kg</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Output</p><strong>{{ $record->output_weight_kg ?? '—' }} kg</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Waste</p><strong>{{ $record->waste_weight_kg ?? '—' }} kg</strong></article></section><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Ordered processing steps</h2></header><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4"># / step</th><th>Status</th><th>Operator</th><th>Measurements</th><th>Timing</th></tr></thead><tbody>@foreach($record->steps as $step)<tr class="border-b"><td class="p-4">{{ $step->sequence }} · {{ $step->type->value }}</td><td>{{ $step->status }}</td><td>{{ $step->performer?->name ?? '—' }}</td><td>@foreach(($step->measurements??[]) as $key=>$value)<span class="mr-3">{{ str_replace('_',' ',$key) }}: {{ $value }}</span>@endforeach</td><td>{{ $step->started_at?->format('Y-m-d H:i') }} → {{ $step->completed_at?->format('Y-m-d H:i') ?? '—' }}</td></tr>@endforeach</tbody></table></section><div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Quality inspections</h2><div class="mt-3 space-y-2">@forelse($record->inspections as $inspection)<a href="{{ route('admin.processor.inspections.show',$inspection) }}" class="flex justify-between rounded border p-3"><span>{{ $inspection->qualityGrade?->code ?? 'No grade' }}</span><span>{{ $inspection->result->value }} · {{ $inspection->product_temperature }} °C</span></a>@empty<p>No inspections.</p>@endforelse</div></section><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Child batches and package labels</h2><div class="mt-3 space-y-2">@forelse($record->batch?->childLinks ?? [] as $link)<div class="rounded border p-3"><a href="{{ route('admin.batches.show',$link->child) }}" class="font-semibold text-cyan-700">{{ $link->child?->batch_code }}</a><div>{{ $link->allocated_weight_kg }} kg · {{ $link->child?->status?->value }}</div>@foreach($link->child?->packageLabels ?? [] as $label)<a href="{{ route('admin.processor.labels.show',$label) }}" class="mr-3 text-sm text-cyan-700">{{ $label->label_code }}</a>@endforeach</div>@empty<p>No child batches.</p>@endforelse</div></section></div></x-layouts.admin>
