<x-layouts.admin title="Processing Records | FishTrace">@include('admin.processor._nav')<header class="flex items-end justify-between"><div><p class="text-sm font-medium text-cyan-700">Processor operations</p><h1 class="text-3xl font-bold">Processing records</h1></div><a href="{{ route('admin.processor.records.export',request()->query()) }}" class="rounded-lg bg-cyan-700 px-4 py-2 text-white">Export CSV</a></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Batch code" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option>@foreach(App\Enums\ProcessingStatus::cases() as $status)<option value="{{ $status->value }}" @selected(($filters['status']??'')===$status->value)>{{ $status->value }}</option>@endforeach</select><select name="processing_type_id" class="rounded-lg border-slate-300"><option value="">All types</option>@foreach($types as $type)<option value="{{ $type->id }}">{{ $type->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Batch</th><th>Processor / type</th><th>Status</th><th>Input → output</th><th>Steps</th><th>Started</th></tr></thead><tbody>@forelse($records as $record)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.processor.records.show',$record) }}" class="font-semibold text-cyan-700">{{ $record->batch?->batch_code }}</a></td><td>{{ $record->organization?->name }}<div>{{ $record->processingType?->name ?? 'Unspecified' }}</div></td><td>{{ $record->status->value }}</td><td>{{ $record->input_weight_kg }} → {{ $record->output_weight_kg ?? '—' }} kg</td><td>{{ $record->steps_count }} · {{ $record->inspections_count }} inspections</td><td>{{ $record->started_at?->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No processing records match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $records->links() }}</div></section></x-layouts.admin>
