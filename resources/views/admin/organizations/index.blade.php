<x-layouts.admin title="Organizations | FishTrace"><header class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-medium text-cyan-700">Access management</p><h1 class="text-3xl font-bold">Organizations</h1><p class="mt-1 text-slate-500">Manage FishTrace operational and regulator tenants.</p></div><a href="{{ route('admin.organizations.create') }}" class="rounded-lg bg-[#075e63] px-4 py-2 font-semibold text-white">Add organization</a></header><form class="mt-7 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Name or code" class="rounded-lg border-slate-300"><select name="type" class="rounded-lg border-slate-300"><option value="">All types</option>@foreach($types as $type)<option @selected(($filters['type'] ?? '')===$type->value)>{{ $type->value }}</option>@endforeach</select><select name="is_active" class="rounded-lg border-slate-300"><option value="">All statuses</option><option value="1" @selected(($filters['is_active'] ?? '')==='1')>Active</option><option value="0" @selected(($filters['is_active'] ?? '')==='0')>Inactive</option></select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b bg-slate-50 text-slate-500"><tr><th class="p-4">Organization</th><th>Type</th><th>Users</th><th>Status</th><th></th></tr></thead><tbody>@forelse($organizations as $organization)<tr class="border-b border-slate-100"><td class="p-4"><a href="{{ route('admin.organizations.show',$organization) }}" class="font-semibold text-[#075e63]">{{ $organization->name }}</a><div class="text-slate-500">{{ $organization->code }}</div></td><td>{{ $organization->type }}</td><td>{{ $organization->users_count }}</td><td>{{ $organization->is_active ? 'ACTIVE' : 'INACTIVE' }}</td><td><a href="{{ route('admin.organizations.edit',$organization) }}" class="text-cyan-700">Edit</a></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">No organizations match these filters.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $organizations->links() }}</div></section></x-layouts.admin>
