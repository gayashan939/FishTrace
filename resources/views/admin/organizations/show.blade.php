<x-layouts.admin title="{{ $organization->name }} | FishTrace"><div class="flex flex-wrap items-start justify-between gap-4"><div><a href="{{ route('admin.organizations.index') }}" class="text-sm text-cyan-700">← Organizations</a><h1 class="mt-2 text-3xl font-bold">{{ $organization->name }}</h1><p class="text-slate-500">{{ $organization->code }} · {{ $organization->type }} · {{ $organization->is_active ? 'ACTIVE' : 'INACTIVE' }}</p></div><div class="flex gap-2"><a href="{{ route('admin.organizations.edit',$organization) }}" class="rounded-lg bg-[#075e63] px-4 py-2 font-semibold text-white">Edit</a>@if($organization->is_active)<form method="post" action="{{ route('admin.organizations.deactivate',$organization) }}">@csrf<button class="rounded-lg border border-red-300 px-4 py-2 text-red-700">Deactivate</button></form>@else<form method="post" action="{{ route('admin.organizations.activate',$organization) }}">@csrf<button class="rounded-lg border border-emerald-300 px-4 py-2 text-emerald-700">Activate</button></form>@endif</div></div><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Assigned users ({{ $organization->users_count }})</h2></header><table class="w-full text-left text-sm"><thead class="border-b bg-slate-50 text-slate-500"><tr><th class="p-4">User</th><th>Roles</th><th>Status</th><th>Primary</th></tr></thead><tbody>@forelse($organization->users as $user)<tr class="border-b border-slate-100"><td class="p-4"><a href="{{ route('admin.users.show',$user) }}" class="font-semibold text-cyan-700">{{ $user->name }}</a><div class="text-slate-500">{{ $user->email }}</div></td><td>{{ $user->roles->pluck('name')->join(', ') }}</td><td>{{ $user->status }}</td><td>{{ $user->pivot->is_primary ? 'Yes' : 'No' }}</td></tr>@empty<tr><td colspan="4" class="p-10 text-center text-slate-500">No users assigned.</td></tr>@endforelse</tbody></table></section></x-layouts.admin>
