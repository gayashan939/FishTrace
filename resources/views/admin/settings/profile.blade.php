<x-layouts.admin title="Administrator profile | FishTrace">
    <header><p class="text-sm font-medium text-cyan-700">Account</p><h1 class="text-3xl font-bold">Administrator profile</h1><p class="mt-1 text-slate-500">Update your own display name and sign-in email without changing roles or organization access.</p></header>
    @include('admin.settings._nav')
    <div class="mt-6 grid gap-6 lg:grid-cols-3">
        <form method="post" action="{{ route('admin.settings.profile.update') }}" class="rounded-xl bg-white p-6 shadow-sm lg:col-span-2">@csrf @method('PUT')<div class="grid gap-5 md:grid-cols-2"><label><span class="text-sm font-medium">Name</span><input name="name" required maxlength="160" value="{{ old('name', $admin->name) }}" class="mt-1 w-full rounded-lg border-slate-300"></label><label><span class="text-sm font-medium">Email</span><input type="email" name="email" required maxlength="160" value="{{ old('email', $admin->email) }}" class="mt-1 w-full rounded-lg border-slate-300"></label></div><button class="mt-7 rounded-lg bg-[#075e63] px-5 py-2.5 font-semibold text-white">Save profile</button></form>
        <aside class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Access</h2><dl class="mt-4 space-y-3 text-sm"><div><dt class="text-slate-500">Roles</dt><dd class="font-medium">{{ $admin->roles->pluck('name')->join(', ') }}</dd></div><div><dt class="text-slate-500">Organizations</dt><dd class="font-medium">{{ $admin->organizations->pluck('name')->join(', ') }}</dd></div><div><dt class="text-slate-500">Last login</dt><dd class="font-medium">{{ $admin->last_login_at?->format('Y-m-d H:i') ?? 'Not recorded' }}</dd></div></dl><p class="mt-5 text-xs text-slate-500">Access assignments are changed through administrator user management and cannot be self-edited.</p></aside>
    </div>
</x-layouts.admin>
