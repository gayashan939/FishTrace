<x-layouts.admin title="Retail Inventory | FishTrace">@include('admin.retail._nav')<header class="flex items-end justify-between"><div><p class="text-sm font-medium text-cyan-700">Retail operations</p><h1 class="text-3xl font-bold">Inventory lots</h1></div><a href="{{ route('admin.retail.inventory.export',request()->query()) }}" class="rounded-lg bg-cyan-700 px-4 py-2 text-white">Export CSV</a></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-5"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Label or batch code" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option>@foreach(App\Enums\InventoryStatus::cases() as $status)<option value="{{ $status->value }}" @selected(($filters['status']??'')===$status->value)>{{ $status->value }}</option>@endforeach</select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All retailers</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><select name="location_id" class="rounded-lg border-slate-300"><option value="">All locations</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Label / batch</th><th>Retailer / location</th><th>Status</th><th>Available / reserved / sold</th><th>Expires</th><th>Ledger</th></tr></thead><tbody>@forelse($lots as $lot)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.retail.inventory.show',$lot) }}" class="font-semibold text-cyan-700">{{ $lot->label?->label_code }}</a><div>{{ $lot->batch?->batch_code }}</div></td><td>{{ $lot->organization?->name }}<div>{{ $lot->location?->name }}</div></td><td>{{ $lot->status->value }}</td><td>{{ $lot->available_packages }} / {{ $lot->reserved_packages }} / {{ $lot->sold_packages }} of {{ $lot->total_packages }}</td><td>{{ $lot->expires_at?->format('Y-m-d') ?? 'Not set' }}</td><td>{{ $lot->movements_count }} movements · {{ $lot->sale_items_count }} sale lines</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No inventory lots match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $lots->links() }}</div></section></x-layouts.admin>
