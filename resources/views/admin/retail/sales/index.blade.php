<x-layouts.admin title="Retail Sales | FishTrace">@include('admin.retail._nav')<header class="flex items-end justify-between"><div><p class="text-sm font-medium text-cyan-700">Retail operations</p><h1 class="text-3xl font-bold">Sales</h1></div><a href="{{ route('admin.retail.sales.export',request()->query()) }}" class="rounded-lg bg-cyan-700 px-4 py-2 text-white">Export CSV</a></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Receipt number" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All retailers</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><select name="location_id" class="rounded-lg border-slate-300"><option value="">All locations</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Receipt</th><th>Retailer / location</th><th>Status</th><th>Items</th><th>Total</th><th>Seller / sold</th></tr></thead><tbody>@forelse($sales as $sale)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.retail.sales.show',$sale) }}" class="font-semibold text-cyan-700">{{ $sale->receipt_number }}</a></td><td>{{ $sale->organization?->name }}<div>{{ $sale->location?->name }}</div></td><td>{{ $sale->status->value }}</td><td>{{ $sale->items_count }}</td><td>{{ $sale->total }}</td><td>{{ $sale->seller?->name }}<div>{{ $sale->sold_at?->format('Y-m-d H:i') }}</div></td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No sales match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $sales->links() }}</div></section></x-layouts.admin>
