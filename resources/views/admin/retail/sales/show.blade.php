<x-layouts.admin title="Sale {{ $sale->receipt_number }} | FishTrace">@include('admin.retail._nav')<a href="{{ route('admin.retail.sales.index') }}" class="text-sm text-cyan-700">← Sales</a><h1 class="mt-2 text-3xl font-bold">{{ $sale->receipt_number }}</h1><p class="text-slate-500">{{ $sale->organization?->name }} · {{ $sale->location?->name }} · {{ $sale->sold_at?->format('Y-m-d H:i') }}</p><section class="mt-6 grid gap-4 md:grid-cols-4"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Status</p><strong>{{ $sale->status->value }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Subtotal</p><strong>{{ $sale->subtotal }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Total</p><strong>{{ $sale->total }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Seller</p><strong>{{ $sale->seller?->name }}</strong></article></section><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Sale items</h2></header><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Label / batch</th><th>Product</th><th>Quantity</th><th>Unit price</th><th>Line total</th></tr></thead><tbody>@foreach($sale->items as $item)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.retail.inventory.show',$item->inventoryLot) }}" class="font-semibold text-cyan-700">{{ $item->inventoryLot?->label?->label_code }}</a><div>{{ $item->inventoryLot?->batch?->batch_code }}</div></td><td>{{ $item->inventoryLot?->batch?->product_type }}</td><td>{{ $item->quantity }}</td><td>{{ $item->unit_price }}</td><td>{{ $item->line_total }}</td></tr>@endforeach</tbody></table></section></x-layouts.admin>
