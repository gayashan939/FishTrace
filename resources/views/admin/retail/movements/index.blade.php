<x-layouts.admin title="Stock Movements | FishTrace">@include('admin.retail._nav')<header><p class="text-sm font-medium text-cyan-700">Retail operations</p><h1 class="text-3xl font-bold">Stock movement ledger</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Label or batch code" class="rounded-lg border-slate-300"><select name="type" class="rounded-lg border-slate-300"><option value="">All movement types</option>@foreach(App\Enums\StockMovementType::cases() as $type)<option value="{{ $type->value }}">{{ $type->value }}</option>@endforeach</select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All retailers</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Occurred</th><th>Label / batch</th><th>Retailer / location</th><th>Movement</th><th>Resulting stock</th><th>Actor / reason</th></tr></thead><tbody>@forelse($movements as $movement)<tr class="border-b"><td class="p-4">{{ $movement->occurred_at?->format('Y-m-d H:i:s') }}</td><td><a href="{{ route('admin.retail.inventory.show',$movement->inventoryLot) }}" class="font-semibold text-cyan-700">{{ $movement->inventoryLot?->label?->label_code }}</a><div>{{ $movement->inventoryLot?->batch?->batch_code }}</div></td><td>{{ $movement->inventoryLot?->organization?->name }}<div>{{ $movement->inventoryLot?->location?->name }}</div></td><td>{{ $movement->type->value }} · {{ $movement->quantity }}</td><td>{{ $movement->resulting_available }} / {{ $movement->resulting_reserved }} / {{ $movement->resulting_sold }}</td><td>{{ $movement->actor?->name }}<div>{{ $movement->reason ?? '—' }}</div></td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No movements match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $movements->links() }}</div></section></x-layouts.admin>
