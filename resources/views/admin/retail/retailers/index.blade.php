<x-layouts.admin title="Retailers | FishTrace">@include('admin.retail._nav')<header><p class="text-sm font-medium text-cyan-700">Retail operations</p><h1 class="text-3xl font-bold">Retailers</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Name or email" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}" @selected(($filters['organization_id']??'')===$org->id)>{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Retailer</th><th>Organization</th><th>Status</th><th>Receipts</th><th>Sales</th></tr></thead><tbody>@forelse($retailers as $retailer)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.retail.retailers.show',$retailer) }}" class="font-semibold text-cyan-700">{{ $retailer->name }}</a><div>{{ $retailer->email }}</div></td><td>{{ $retailer->organizations->first()?->name }}</td><td>{{ $retailer->status }}</td><td>{{ $retailer->retail_receipts_count }}</td><td>{{ $retailer->retail_sales_count }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center">No retailers match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $retailers->links() }}</div></section></x-layouts.admin>
