<x-layouts.admin title="{{ $location->name }} | FishTrace">@include('admin.retail._nav')<a href="{{ route('admin.retail.locations.index') }}" class="text-sm text-cyan-700">← Locations</a><h1 class="mt-2 text-3xl font-bold">{{ $location->name }}</h1><p class="text-slate-500">{{ $location->organization?->name }} · {{ $location->code }} · {{ $location->address }}</p><section class="mt-6 grid gap-4 md:grid-cols-3"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Receipts</p><strong>{{ $location->receipts_count }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Inventory lots</p><strong>{{ $location->inventory_lots_count }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Sales</p><strong>{{ $location->sales_count }}</strong></article></section><div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Newest 100 inventory lots</h2><div class="mt-3 space-y-2">@foreach($location->inventoryLots as $lot)<a href="{{ route('admin.retail.inventory.show',$lot) }}" class="flex justify-between rounded border p-3"><span>{{ $lot->label?->label_code }}</span><span>{{ $lot->status->value }} · {{ $lot->available_packages }} available</span></a>@endforeach</div></section><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Newest 100 sales</h2><div class="mt-3 space-y-2">@forelse($location->sales as $sale)<a href="{{ route('admin.retail.sales.show',$sale) }}" class="flex justify-between rounded border p-3"><span>{{ $sale->receipt_number }}</span><span>{{ $sale->total }}</span></a>@empty<p>No sales recorded.</p>@endforelse</div></section></div></x-layouts.admin>
