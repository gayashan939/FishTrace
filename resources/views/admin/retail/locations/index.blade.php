<x-layouts.admin title="Retail Locations | FishTrace">@include('admin.retail._nav')<header><p class="text-sm font-medium text-cyan-700">Retail operations</p><h1 class="text-3xl font-bold">Locations</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Name, code, or address" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option><option>ACTIVE</option><option>INACTIVE</option></select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Location</th><th>Organization</th><th>Status</th><th>Receipts</th><th>Inventory</th><th>Sales</th></tr></thead><tbody>@forelse($locations as $location)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.retail.locations.show',$location) }}" class="font-semibold text-cyan-700">{{ $location->name }}</a><div>{{ $location->code }} · {{ $location->address }}</div></td><td>{{ $location->organization?->name }}</td><td>{{ $location->is_active?'ACTIVE':'INACTIVE' }}</td><td>{{ $location->receipts_count }}</td><td>{{ $location->inventory_lots_count }}</td><td>{{ $location->sales_count }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No locations match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $locations->links() }}</div></section></x-layouts.admin>
