<x-layouts.admin title="Retail Receipts | FishTrace">@include('admin.retail._nav')<header><p class="text-sm font-medium text-cyan-700">Retail operations</p><h1 class="text-3xl font-bold">Package receipts</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Label or batch code" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><select name="location_id" class="rounded-lg border-slate-300"><option value="">All locations</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Label / batch</th><th>Retailer / location</th><th>Packages / weight</th><th>Condition</th><th>Received</th><th>Inventory</th></tr></thead><tbody>@forelse($receipts as $receipt)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.retail.receipts.show',$receipt) }}" class="font-semibold text-cyan-700">{{ $receipt->label?->label_code }}</a><div>{{ $receipt->batch?->batch_code }}</div></td><td>{{ $receipt->retailerOrganization?->name }}<div>{{ $receipt->location?->name }}</div></td><td>{{ $receipt->received_package_count }} / {{ $receipt->received_weight_kg }} kg</td><td>{{ $receipt->condition_temperature ?? '—' }} °C</td><td>{{ $receipt->received_at?->format('Y-m-d H:i') }}</td><td>{{ $receipt->inventoryLot?->status?->value ?? '—' }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No receipts match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $receipts->links() }}</div></section></x-layouts.admin>
