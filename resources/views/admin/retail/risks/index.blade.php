<x-layouts.admin title="Expiry and Recalls | FishTrace">@include('admin.retail._nav')<header><p class="text-sm font-medium text-cyan-700">Retail operations</p><h1 class="text-3xl font-bold">Expiry and recall monitoring</h1><p class="mt-1 text-slate-500">Recalled, expired, and inventory expiring within seven days.</p></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Label or batch code" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All retailers</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Label / batch</th><th>Retailer / location</th><th>Status</th><th>Expires</th><th>Available stock</th><th>Risk</th></tr></thead><tbody>@forelse($lots as $lot)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.retail.inventory.show',$lot) }}" class="font-semibold text-cyan-700">{{ $lot->label?->label_code }}</a><div>{{ $lot->batch?->batch_code }}</div></td><td>{{ $lot->organization?->name }}<div>{{ $lot->location?->name }}</div></td><td>{{ $lot->status->value }}</td><td>{{ $lot->expires_at?->format('Y-m-d H:i') ?? 'Not set' }}</td><td>{{ $lot->available_packages }}</td><td>{{ $lot->status->value==='RECALLED'?'RECALL':($lot->expires_at?->isPast()?'EXPIRED':'EXPIRING SOON') }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No active expiry or recall risks.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $lots->links() }}</div></section></x-layouts.admin>
