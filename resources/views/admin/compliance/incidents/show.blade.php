<x-layouts.admin title="Incident {{ $inspection->batch?->batch_code }} | FishTrace">@include('admin.compliance._nav')<a href="{{ route('admin.compliance.incidents.index') }}" class="text-sm text-cyan-700">← Quality incidents</a><h1 class="mt-2 text-3xl font-bold">{{ $inspection->batch?->batch_code }}</h1><p class="text-slate-500">{{ $inspection->result->value }} inspection · {{ $inspection->organization?->name }}</p><section class="mt-6 grid gap-4 md:grid-cols-4"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Grade</p><strong>{{ $inspection->qualityGrade?->code ?? '—' }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Temperature</p><strong>{{ $inspection->product_temperature }} °C</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>pH</p><strong>{{ $inspection->ph_level ?? '—' }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Inspector</p><strong>{{ $inspection->inspector?->name }}</strong></article></section><div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Findings</h2><p class="mt-3">Appearance: {{ $inspection->appearance }}</p><p>Odor: {{ $inspection->odor }}</p><p>Notes: {{ $inspection->notes ?? 'No notes recorded.' }}</p><div class="mt-4 flex gap-4"><a href="{{ route('admin.batches.show',$inspection->batch) }}" class="text-cyan-700">Open traceability record</a>@if($inspection->processingRecord)<a href="{{ route('admin.processor.records.show',$inspection->processingRecord) }}" class="text-cyan-700">Open processing</a>@endif</div></section><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Affected retail custody</h2><div class="mt-3 space-y-2">@forelse($affectedLots as $lot)<a href="{{ route('admin.retail.inventory.show',$lot) }}" class="block rounded border p-3">{{ $lot->label?->label_code }} · {{ $lot->organization?->name }} · {{ $lot->status->value }}</a>@empty<p>No retail inventory is linked.</p>@endforelse</div></section></div><section class="mt-6 rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Evidence files</h2><div class="mt-3 space-y-2">@forelse($evidence as $file)<div class="flex justify-between rounded border p-3"><span>{{ $file->original_name }} · {{ $file->category->value }}</span><a href="{{ route('admin.compliance.evidence.download',$file) }}" class="text-cyan-700">Authorized download</a></div>@empty<p>No evidence attached.</p>@endforelse</div></section></x-layouts.admin>
