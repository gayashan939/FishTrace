<x-layouts.admin title="Quality Incidents | FishTrace">@include('admin.compliance._nav')<header class="flex items-end justify-between"><div><p class="text-sm font-medium text-cyan-700">Compliance & reporting</p><h1 class="text-3xl font-bold">Quality incident queue</h1></div><a href="{{ route('admin.compliance.incidents.export',request()->query()) }}" class="rounded-lg bg-cyan-700 px-4 py-2 text-white">Export CSV</a></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Batch code" class="rounded-lg border-slate-300"><select name="result" class="rounded-lg border-slate-300"><option value="">Failed and conditional</option><option>FAILED</option><option>CONDITIONAL</option></select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Batch</th><th>Processor</th><th>Result / grade</th><th>Temperature / pH</th><th>Inspector</th><th>Inspected</th><th>Recall</th></tr></thead><tbody>@forelse($incidents as $incident)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.compliance.incidents.show',$incident) }}" class="font-semibold text-cyan-700">{{ $incident->batch?->batch_code }}</a></td><td>{{ $incident->organization?->name }}</td><td>{{ $incident->result->value }}<div>{{ $incident->qualityGrade?->code ?? '—' }}</div></td><td>{{ $incident->product_temperature }} °C / {{ $incident->ph_level ?? '—' }}</td><td>{{ $incident->inspector?->name }}</td><td>{{ $incident->inspected_at?->format('Y-m-d H:i') }}</td><td>{{ $incident->batch?->is_recalled?'RECALLED':'Not recalled' }}</td></tr>@empty<tr><td colspan="7" class="p-10 text-center">No quality incidents match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $incidents->links() }}</div></section></x-layouts.admin>
