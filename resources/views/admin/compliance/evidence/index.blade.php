<x-layouts.admin title="Evidence Files | FishTrace">@include('admin.compliance._nav')<header><p class="text-sm font-medium text-cyan-700">Compliance & reporting</p><h1 class="text-3xl font-bold">Evidence files</h1><p class="mt-1 text-slate-500">Private storage paths and checksums are not displayed.</p></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Original file name" class="rounded-lg border-slate-300"><select name="category" class="rounded-lg border-slate-300"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->value }}">{{ $category->value }}</option>@endforeach</select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">File</th><th>Category / entity</th><th>Organization</th><th>Uploader</th><th>Type / size</th><th>Uploaded</th><th></th></tr></thead><tbody>@forelse($files as $file)<tr class="border-b"><td class="p-4 font-semibold">{{ $file->original_name }}</td><td>{{ $file->category->value }}<div>{{ $file->entity_type ?? 'Unlinked' }}</div></td><td>{{ $file->organization?->name }}</td><td>{{ $file->uploader?->name }}</td><td>{{ $file->mime_type }} · {{ number_format($file->size_bytes/1024,1) }} KB</td><td>{{ $file->created_at?->format('Y-m-d H:i') }}</td><td><a href="{{ route('admin.compliance.evidence.download',$file) }}" class="text-cyan-700">Download</a></td></tr>@empty<tr><td colspan="7" class="p-10 text-center">No evidence files match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $files->links() }}</div></section></x-layouts.admin>
