<x-layouts.admin title="Blockchain Verification | FishTrace">@include('admin.compliance._nav')<header><p class="text-sm font-medium text-cyan-700">Compliance & reporting</p><h1 class="text-3xl font-bold">Blockchain anchors</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Transaction reference or hash" class="rounded-lg border-slate-300"><input name="status" value="{{ $filters['status'] ?? '' }}" placeholder="Status" class="rounded-lg border-slate-300"><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Transaction</th><th>Batch / event</th><th>Network</th><th>Status</th><th>Attempts</th><th>Verifications</th><th>Timing</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.compliance.blockchain.show',$transaction) }}" class="font-semibold text-cyan-700">{{ $transaction->transaction_reference ?? 'Pending reference' }}</a><div class="font-mono text-xs">{{ substr($transaction->event_hash,0,16) }}…</div></td><td>{{ $transaction->batch_code ?? 'Unlinked' }}<div>{{ $transaction->event_type }}</div></td><td>{{ $transaction->network }}</td><td>{{ $transaction->status }}</td><td>{{ $transaction->attempts }}</td><td>{{ $transaction->verifications_count }}</td><td>{{ $transaction->submitted_at?->format('Y-m-d H:i') ?? '—' }}<div>{{ $transaction->confirmed_at?->format('Y-m-d H:i') ?? 'Not confirmed' }}</div></td></tr>@empty<tr><td colspan="7" class="p-10 text-center">No blockchain transactions match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $transactions->links() }}</div></section></x-layouts.admin>
