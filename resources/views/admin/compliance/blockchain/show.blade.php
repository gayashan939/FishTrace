<x-layouts.admin title="Blockchain Anchor | FishTrace">@include('admin.compliance._nav')<a href="{{ route('admin.compliance.blockchain.index') }}" class="text-sm text-cyan-700">← Blockchain anchors</a><h1 class="mt-2 text-3xl font-bold">{{ $transaction->transaction_reference ?? 'Pending anchor' }}</h1><p class="text-slate-500">{{ $transaction->network }} · {{ $transaction->status }}</p><section class="mt-6 grid gap-4 md:grid-cols-4"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Attempts</p><strong>{{ $transaction->attempts }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Submitted</p><strong>{{ $transaction->submitted_at?->format('Y-m-d H:i') ?? '—' }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Confirmed</p><strong>{{ $transaction->confirmed_at?->format('Y-m-d H:i') ?? '—' }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Verifications</p><strong>{{ $transaction->verifications->count() }}</strong></article></section><section class="mt-6 rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Anchored event</h2>@if($anchor)<p class="mt-3"><a href="{{ route('admin.batches.show',$anchor->batch_id) }}" class="text-cyan-700">{{ $anchor->batch_code }}</a> · {{ $anchor->event_type }} · {{ $anchor->title }}</p><p>{{ $anchor->occurred_at }}</p>@else<p class="mt-3">No traceability anchor link found.</p>@endif<p class="mt-4 break-all font-mono text-xs">Event hash: {{ $transaction->event_hash }}</p></section><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Latest 100 verification attempts</h2><p class="text-sm text-slate-500">Provider response bodies are withheld.</p></header><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Verified</th><th>Result</th></tr></thead><tbody>@forelse($transaction->verifications as $verification)<tr class="border-b"><td class="p-4">{{ $verification->verified_at?->format('Y-m-d H:i:s') }}</td><td>{{ $verification->is_valid?'VALID':'INVALID' }}</td></tr>@empty<tr><td colspan="2" class="p-10 text-center">No verification attempts.</td></tr>@endforelse</tbody></table></section></x-layouts.admin>
