<x-layouts.admin title="Notification Delivery | FishTrace">@include('admin.compliance._nav')<header><p class="text-sm font-medium text-cyan-700">Compliance & reporting</p><h1 class="text-3xl font-bold">Notification delivery</h1><p class="mt-1 text-slate-500">Operational message summaries only; internal context payloads are excluded.</p></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-5"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Recipient name or email" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">All types</option>@foreach($types as $type)<option value="{{ $type->value }}">{{ $type->value }}</option>@endforeach</select><select name="read" class="rounded-lg border-slate-300"><option value="">Read and unread</option><option value="unread">Unread</option><option value="read">Read</option></select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Created</th><th>Type</th><th>Recipient</th><th>Organization</th><th>Message</th><th>State</th></tr></thead><tbody>@forelse($notifications as $notification)<tr class="border-b"><td class="p-4">{{ $notification->created_at?->format('Y-m-d H:i') }}</td><td>{{ $notification->data['type'] ?? 'UNKNOWN' }}</td><td>{{ $notification->recipient_name }}<div>{{ $notification->recipient_email }}</div></td><td>{{ $notification->organization_name }}</td><td><strong>{{ $notification->data['title'] ?? 'Notification' }}</strong><div>{{ $notification->data['message'] ?? '' }}</div></td><td>{{ $notification->read_at?'READ':'UNREAD' }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No notifications match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $notifications->links() }}</div></section></x-layouts.admin>
