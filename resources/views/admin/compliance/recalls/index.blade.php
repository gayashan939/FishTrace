<x-layouts.admin title="Recall Monitoring | FishTrace">@include('admin.compliance._nav')<header class="flex items-end justify-between"><div><p class="text-sm font-medium text-cyan-700">Compliance & reporting</p><h1 class="text-3xl font-bold">Recall monitoring</h1></div><a href="{{ route('admin.compliance.recalls.export',request()->query()) }}" class="rounded-lg bg-cyan-700 px-4 py-2 text-white">Export CSV</a></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Batch code" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All affected organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Batch</th><th>Source</th><th>Batch status</th><th>Batch flag</th><th>Recalled custody lots</th><th>Trace events</th><th>Affected retailers</th></tr></thead><tbody>@forelse($batches as $batch)<tr class="border-b"><td class="p-4"><a href="{{ route('admin.batches.show',$batch) }}" class="font-semibold text-cyan-700">{{ $batch->batch_code }}</a><div>{{ $batch->species?->common_name }}</div></td><td>{{ $batch->organization?->name }}</td><td>{{ $batch->status->value }}</td><td>{{ $batch->is_recalled?'RECALLED':'Downstream recall' }}</td><td>{{ $batch->recalled_lots_count }}</td><td>{{ $batch->events_count }}</td><td>@foreach($batch->inventoryLots as $lot)<a href="{{ route('admin.retail.inventory.show',$lot) }}" class="block text-cyan-700">{{ $lot->organization?->name }} · {{ $lot->location?->name }} · {{ $lot->label?->label_code }}</a>@endforeach</td></tr>@empty<tr><td colspan="7" class="p-10 text-center">No recalls match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $batches->links() }}</div></section></x-layouts.admin>
