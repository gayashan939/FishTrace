<x-layouts.admin title="Report Exports | FishTrace">@include('admin.compliance._nav')<header><p class="text-sm font-medium text-cyan-700">Compliance & reporting</p><h1 class="text-3xl font-bold">Queued report exports</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Report type" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option>@foreach(App\Enums\ReportExportStatus::cases() as $status)<option value="{{ $status->value }}">{{ $status->value }}</option>@endforeach</select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $org)<option value="{{ $org->id }}">{{ $org->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Requested</th><th>Report</th><th>Organization</th><th>Requester</th><th>Status</th><th>Timing</th><th>Artifact</th></tr></thead><tbody>@forelse($exports as $export)<tr class="border-b"><td class="p-4">{{ $export->created_at?->format('Y-m-d H:i') }}</td><td>{{ $export->report_type->value }}</td><td>{{ $export->organization?->name }}</td><td>{{ $export->requester?->name }}</td><td>{{ $export->status->value }}</td><td>{{ $export->started_at?->format('Y-m-d H:i') ?? '—' }} → {{ $export->completed_at?->format('Y-m-d H:i') ?? '—' }}</td><td>@if($export->file)<a href="{{ route('admin.compliance.evidence.download',$export->file) }}" class="text-cyan-700">{{ $export->file->original_name }}</a>@else No artifact @endif</td></tr>@empty<tr><td colspan="7" class="p-10 text-center">No report exports match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $exports->links() }}</div></section></x-layouts.admin>
