<x-layouts.admin title="{{ $fisher->name }} | FishTrace">
    @include('admin.fishing._nav')
    <a href="{{ route('admin.fishing.fishers.index') }}" class="text-sm text-cyan-700">← Fishers</a><h1 class="mt-2 text-3xl font-bold">{{ $fisher->name }}</h1><p class="text-slate-500">{{ $fisher->email }}</p>
    <section class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Status</p><p class="mt-2 font-bold">{{ $fisher->isLocked() ? 'LOCKED' : $fisher->status }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Organizations</p><p class="mt-2 font-bold">{{ $fisher->organizations->pluck('name')->join(', ') }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Boats</p><p class="mt-2 text-2xl font-bold">{{ $fisher->owned_boats_count }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Trips</p><p class="mt-2 text-2xl font-bold">{{ $fisher->fishing_trips_count }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Recorded catch</p><p class="mt-2 text-2xl font-bold">{{ number_format((float) ($catchStats->total_weight_kg ?? 0), 3) }} kg</p><p class="text-xs text-slate-500">{{ $catchStats->catch_count ?? 0 }} records</p></article></section>
    <div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Owned boats</h2><div class="mt-3 space-y-2">@forelse ($fisher->ownedBoats as $boat)<a href="{{ route('admin.fishing.boats.show', $boat) }}" class="flex justify-between rounded-lg border border-slate-200 p-3"><span>{{ $boat->name }} <small class="text-slate-500">{{ $boat->registration_number }}</small></span><span>{{ $boat->is_active ? 'ACTIVE' : 'INACTIVE' }}</span></a>@empty<p class="text-slate-500">No boats owned.</p>@endforelse</div></section><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Recent trips</h2><div class="mt-3 space-y-2">@forelse ($fisher->fishingTrips->sortByDesc('created_at')->take(20) as $trip)<a href="{{ route('admin.fishing.trips.show', $trip) }}" class="flex justify-between rounded-lg border border-slate-200 p-3"><span>{{ $trip->trip_code }} <small class="text-slate-500">{{ $trip->boat?->name }}</small></span><span>{{ $trip->status->value }}</span></a>@empty<p class="text-slate-500">No fishing trips.</p>@endforelse</div></section></div>
</x-layouts.admin>
