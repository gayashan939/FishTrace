<x-layouts.admin title="{{ $trip->trip_code }} | FishTrace">
    @include('admin.fishing._nav')
    <a href="{{ route('admin.fishing.trips.index') }}" class="text-sm text-cyan-700">← Fishing trips</a><h1 class="mt-2 text-3xl font-bold">{{ $trip->trip_code }}</h1><p class="text-slate-500">{{ $trip->general_catch_area ?? 'Catch area not recorded' }}</p>
    <section class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Status</p><p class="mt-2 font-bold">{{ $trip->status->value }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Fisher</p><a href="{{ route('admin.fishing.fishers.show', $trip->fisher) }}" class="mt-2 block font-bold text-cyan-700">{{ $trip->fisher?->name }}</a></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Boat</p><a href="{{ route('admin.fishing.boats.show', $trip->boat) }}" class="mt-2 block font-bold text-cyan-700">{{ $trip->boat?->name }}</a><p class="text-xs text-slate-500">{{ $trip->boat?->registration_number }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Landing site</p><p class="mt-2 font-bold">{{ $trip->landingSite?->name ?? '—' }}</p><p class="text-xs text-slate-500">{{ $trip->landingSite?->district }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Voyage</p><p class="mt-2 text-sm font-semibold">{{ $trip->departed_at?->format('Y-m-d H:i') ?? 'Not departed' }}</p><p class="text-xs text-slate-500">to {{ $trip->returned_at?->format('Y-m-d H:i') ?? 'not returned' }}</p></article></section>
    @if ($totals['overallocated'] > 0)<div class="mt-6 rounded-xl border border-red-300 bg-red-50 p-4 font-semibold text-red-900">Allocation integrity warning: {{ number_format($totals['overallocated'], 3) }} kg is overallocated.</div>@elseif ($totals['unallocated'] > 0)<div class="mt-6 rounded-xl border border-amber-300 bg-amber-50 p-4 font-semibold text-amber-900">{{ number_format($totals['unallocated'], 3) }} kg remains available for batch allocation.</div>@endif
    <section class="mt-6 grid gap-4 md:grid-cols-4"><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Catch records</p><p class="mt-2 text-2xl font-bold">{{ $trip->catches->count() }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Catch weight</p><p class="mt-2 text-2xl font-bold">{{ number_format($totals['weight'], 3) }} kg</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Ledger allocated</p><p class="mt-2 text-2xl font-bold">{{ number_format($totals['ledger_allocated'], 3) }} kg</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Unallocated</p><p class="mt-2 text-2xl font-bold">{{ number_format($totals['unallocated'], 3) }} kg</p></article></section>
    <section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Catch allocation reconciliation</h2></header><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b bg-slate-50 text-slate-500"><tr><th class="p-4">Catch</th><th>Species and gear</th><th>Weight</th><th>Ledger allocated</th><th>Available</th><th>Integrity</th></tr></thead><tbody>@forelse ($trip->catches as $catch)@php($item = $reconciliation->get($catch->id))<tr class="border-b border-slate-100"><td class="p-4"><a href="{{ route('admin.fishing.catches.show', $catch) }}" class="font-semibold text-cyan-700">{{ $catch->caught_at?->format('Y-m-d H:i') }}</a><div class="text-slate-500">{{ $catch->quantity }} fish</div></td><td>{{ $catch->species?->common_name }}<div class="text-slate-500">{{ $catch->gearType?->name ?? 'Gear not recorded' }}</div></td><td>{{ number_format($item['weight'], 3) }} kg</td><td>{{ number_format($item['ledger_allocated'], 3) }} kg</td><td>{{ number_format($item['unallocated'], 3) }} kg</td><td><span class="font-semibold {{ $item['overallocated'] > 0 || $item['mismatch'] ? 'text-red-700' : 'text-emerald-700' }}">{{ $item['status'] }}{{ $item['mismatch'] ? ' · STORED MISMATCH' : '' }}</span></td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-500">No catches recorded for this trip.</td></tr>@endforelse</tbody></table></div></section>
</x-layouts.admin>
