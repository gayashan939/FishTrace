<x-layouts.admin title="Fishing Trips | FishTrace">
    @include('admin.fishing._nav')
    <header class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-medium text-cyan-700">Fishing operations</p><h1 class="text-3xl font-bold">Fishing trips</h1><p class="mt-1 text-slate-500">Monitor voyage state, catch totals, allocation, vessels, and landing sites.</p></div><a href="{{ route('admin.fishing.trips.export', request()->query()) }}" class="rounded-lg border border-cyan-700 px-4 py-2 font-semibold text-cyan-800">Export CSV</a></header>
    <form class="mt-6 rounded-xl bg-white p-4 shadow-sm"><div class="grid gap-3 md:grid-cols-3 xl:grid-cols-6"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Trip code or catch area" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option>@foreach (App\Enums\FishingTripStatus::cases() as $status)<option value="{{ $status->value }}" @selected(($filters['status'] ?? '') === $status->value)>{{ $status->value }}</option>@endforeach</select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All cooperatives</option>@foreach ($organizations as $organization)<option value="{{ $organization->id }}" @selected(($filters['organization_id'] ?? '') === $organization->id)>{{ $organization->name }}</option>@endforeach</select><select name="fisher_id" class="rounded-lg border-slate-300"><option value="">All fishers</option>@foreach ($fishers as $fisher)<option value="{{ $fisher->id }}" @selected(($filters['fisher_id'] ?? '') === $fisher->id)>{{ $fisher->name }}</option>@endforeach</select><input type="date" name="date_from" value="{{ $filters['date_from'] ?? '' }}" class="rounded-lg border-slate-300" aria-label="Departed from"><div class="flex gap-2"><button class="flex-1 rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button><a href="{{ route('admin.fishing.trips.index') }}" class="rounded-lg border border-slate-300 px-4 py-2">Clear</a></div></div></form>
    <section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b bg-slate-50 text-slate-500"><tr><th class="p-4">Trip</th><th>Fisher and boat</th><th>Status</th><th>Landing</th><th>Catches</th><th>Allocation</th></tr></thead><tbody>@forelse ($trips as $trip)<tr class="border-b border-slate-100"><td class="p-4"><a href="{{ route('admin.fishing.trips.show', $trip) }}" class="font-semibold text-cyan-700">{{ $trip->trip_code }}</a><div class="text-slate-500">{{ $trip->organization?->name }}</div></td><td>{{ $trip->fisher?->name }}<div class="text-slate-500">{{ $trip->boat?->name }} · {{ $trip->boat?->registration_number }}</div></td><td>{{ $trip->status->value }}</td><td>{{ $trip->landingSite?->name ?? '—' }}<div class="text-slate-500">{{ $trip->departed_at?->format('Y-m-d H:i') ?? 'Not departed' }}</div></td><td>{{ $trip->catches_count }} · {{ number_format((float) ($trip->catches_sum_weight_kg ?? 0), 3) }} kg</td><td>{{ number_format((float) ($trip->catches_sum_allocated_weight_kg ?? 0), 3) }} kg stored</td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-500">No fishing trips match these filters.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $trips->links() }}</div></section>
</x-layouts.admin>
