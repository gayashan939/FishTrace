<x-layouts.admin title="Boats | FishTrace">
    @include('admin.fishing._nav')
    <header><p class="text-sm font-medium text-cyan-700">Fishing operations</p><h1 class="text-3xl font-bold">Boat registry</h1><p class="mt-1 text-slate-500">Vessel ownership, capacity, availability, and trip activity.</p></header>
    <form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Name or registration" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All cooperatives</option>@foreach ($organizations as $organization)<option value="{{ $organization->id }}" @selected(($filters['organization_id'] ?? '') === $organization->id)>{{ $organization->name }}</option>@endforeach</select><select name="fisher_id" class="rounded-lg border-slate-300"><option value="">All owners</option>@foreach ($fishers as $fisher)<option value="{{ $fisher->id }}" @selected(($filters['fisher_id'] ?? '') === $fisher->id)>{{ $fisher->name }}</option>@endforeach</select><div class="flex gap-2"><button class="flex-1 rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button><a href="{{ route('admin.fishing.boats.index') }}" class="rounded-lg border border-slate-300 px-4 py-2">Clear</a></div></form>
    <section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b bg-slate-50 text-slate-500"><tr><th class="p-4">Boat</th><th>Owner</th><th>Organization</th><th>Capacity</th><th>Trips</th><th>Status</th></tr></thead><tbody>@forelse ($boats as $boat)<tr class="border-b border-slate-100"><td class="p-4"><a href="{{ route('admin.fishing.boats.show', $boat) }}" class="font-semibold text-cyan-700">{{ $boat->name }}</a><div class="text-slate-500">{{ $boat->registration_number }} · {{ $boat->type }}</div></td><td>{{ $boat->owner?->name }}</td><td>{{ $boat->organization?->name }}</td><td>{{ $boat->capacity_kg ? number_format((float) $boat->capacity_kg, 3).' kg' : '—' }}</td><td>{{ $boat->trips_count }}</td><td>{{ $boat->is_active ? 'ACTIVE' : 'INACTIVE' }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-500">No boats match these filters.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $boats->links() }}</div></section>
</x-layouts.admin>
