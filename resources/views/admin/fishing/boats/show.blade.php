<x-layouts.admin title="{{ $boat->name }} | FishTrace">
    @include('admin.fishing._nav')
    <a href="{{ route('admin.fishing.boats.index') }}" class="text-sm text-cyan-700">← Boats</a><h1 class="mt-2 text-3xl font-bold">{{ $boat->name }}</h1><p class="text-slate-500">{{ $boat->registration_number }} · {{ $boat->type ?? 'Unspecified type' }}</p>
    <section class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Status</p><p class="mt-2 font-bold">{{ $boat->is_active ? 'ACTIVE' : 'INACTIVE' }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Owner</p><a href="{{ route('admin.fishing.fishers.show', $boat->owner) }}" class="mt-2 block font-bold text-cyan-700">{{ $boat->owner?->name }}</a></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Capacity</p><p class="mt-2 text-xl font-bold">{{ $boat->capacity_kg ? number_format((float) $boat->capacity_kg, 3).' kg' : '—' }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Trips</p><p class="mt-2 text-xl font-bold">{{ $boat->trips_count }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Recorded catch</p><p class="mt-2 text-xl font-bold">{{ number_format((float) ($stats->total_weight_kg ?? 0), 3) }} kg</p></article></section>
    <section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Trip history <span class="text-sm font-normal text-slate-500">(newest 100)</span></h2></header><table class="w-full text-left text-sm"><thead class="border-b bg-slate-50 text-slate-500"><tr><th class="p-4">Trip</th><th>Fisher</th><th>Status</th><th>Landing site</th><th>Catches</th></tr></thead><tbody>@forelse ($boat->trips as $trip)<tr class="border-b border-slate-100"><td class="p-4"><a href="{{ route('admin.fishing.trips.show', $trip) }}" class="font-semibold text-cyan-700">{{ $trip->trip_code }}</a></td><td>{{ $trip->fisher?->name }}</td><td>{{ $trip->status->value }}</td><td>{{ $trip->landingSite?->name ?? '—' }}</td><td>{{ $trip->catches_count }} · {{ number_format((float) ($trip->catches_sum_weight_kg ?? 0), 3) }} kg</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">No trips recorded.</td></tr>@endforelse</tbody></table></section>
</x-layouts.admin>
