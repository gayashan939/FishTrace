<x-layouts.admin title="{{ $trip->trip_code }} | FishTrace">@include('admin.transport._nav')<a href="{{ route('admin.transport.trips.index') }}" class="text-sm text-cyan-700">← Transport trips</a><h1 class="mt-2 text-3xl font-bold">{{ $trip->trip_code }}</h1><p class="text-slate-500">{{ $trip->origin }} → {{ $trip->destination }} · {{ $trip->driver_name }}</p><section class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><article class="rounded-xl bg-white p-5 shadow-sm"><p>Status</p><strong>{{ $trip->status->value }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Vehicle</p><a class="font-bold text-cyan-700" href="{{ route('admin.transport.vehicles.show',$trip->vehicle) }}">{{ $trip->vehicle?->registration_number }}</a></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Readings</p><strong>{{ $stats->reading_count ?? 0 }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Temperature</p><strong>{{ $stats?number_format((float)$stats->minimum_temperature,2).'–'.number_format((float)$stats->maximum_temperature,2).' °C':'—' }}</strong></article><article class="rounded-xl bg-white p-5 shadow-sm"><p>Open alerts</p><strong>{{ $trip->alerts->whereIn('status',['OPEN','ACKNOWLEDGED'])->count() }}</strong></article></section><div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Batches</h2><div class="mt-3 space-y-2">@foreach($trip->batches as $batch)<a href="{{ route('admin.batches.show',$batch) }}" class="flex justify-between rounded border p-3"><span>{{ $batch->batch_code }}</span><span>{{ $batch->status->value }} · {{ $batch->total_weight_kg }} kg</span></a>@endforeach</div></section><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Device assignments</h2><div class="mt-3 space-y-2">@foreach($trip->assignments as $assignment)<div class="rounded border p-3"><a href="{{ route('admin.transport.devices.show',$assignment->device) }}" class="font-semibold text-cyan-700">{{ $assignment->device?->display_name }}</a><p class="text-sm">{{ $assignment->status }} · Firebase {{ $assignment->firebase_sync_status }} · {{ $assignment->assigned_at?->format('Y-m-d H:i') }}</p></div>@endforeach</div></section></div><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Latest 250 permanent readings</h2></header><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Recorded</th><th>Product / air</th><th>Humidity</th><th>Battery / signal</th><th>Door</th></tr></thead><tbody>@foreach($trip->readings as $reading)<tr class="border-b"><td class="p-4">{{ $reading->recorded_at?->format('Y-m-d H:i:s') }}</td><td>{{ $reading->product_temperature }} / {{ $reading->air_temperature }} °C</td><td>{{ $reading->humidity }}%</td><td>{{ $reading->battery_percentage }}% / {{ $reading->signal_strength }}</td><td>{{ $reading->door_open?'OPEN':'CLOSED' }}</td></tr>@endforeach</tbody></table></section><section class="mt-6 rounded-xl bg-white p-6 shadow-sm"><h2 class="font-bold">Cold-chain alerts</h2><div class="mt-3 space-y-2">@foreach($trip->alerts as $alert)<a href="{{ route('admin.transport.alerts.show',$alert) }}" class="flex justify-between rounded border p-3"><span>{{ str_replace('_',' ',$alert->type) }}</span><span>{{ $alert->severity }} · {{ $alert->status }}</span></a>@endforeach</div></section></x-layouts.admin>
