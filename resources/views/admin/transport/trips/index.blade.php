<x-layouts.admin title="Transport Trips | FishTrace">@include('admin.transport._nav')<header><p class="text-sm font-medium text-cyan-700">Transport & IoT</p><h1 class="text-3xl font-bold">Transport trips</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Trip, driver, or route" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option>@foreach(App\Enums\TransportTripStatus::cases() as $status)<option value="{{ $status->value }}">{{ $status->value }}</option>@endforeach</select><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $organization)<option value="{{ $organization->id }}">{{ $organization->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Trip</th><th>Route</th><th>Status</th><th>Vehicle / device</th><th>Temperature</th><th>Records</th></tr></thead><tbody>@forelse($trips as $trip)<tr class="border-b"><td class="p-4"><a class="font-semibold text-cyan-700" href="{{ route('admin.transport.trips.show',$trip) }}">{{ $trip->trip_code }}</a><div class="text-slate-500">{{ $trip->driver_name }}</div></td><td>{{ $trip->origin }} → {{ $trip->destination }}</td><td>{{ $trip->status->value }}</td><td>{{ $trip->vehicle?->registration_number }}<div>{{ $trip->activeAssignment?->device?->device_code ?? 'No active device' }}</div></td><td>{{ $trip->readings_count ? $trip->readings_min_product_temperature.'–'.$trip->readings_max_product_temperature.' °C':'—' }}</td><td>{{ $trip->batches_count }} batches · {{ $trip->readings_count }} readings · {{ $trip->alerts_count }} alerts</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No trips match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $trips->links() }}</div></section></x-layouts.admin>
