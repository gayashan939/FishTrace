<x-layouts.admin title="Cold-chain Alerts | FishTrace">@include('admin.transport._nav')<header class="flex justify-between gap-4"><div><p class="text-sm font-medium text-cyan-700">Transport & IoT</p><h1 class="text-3xl font-bold">Cold-chain alerts</h1></div><a href="{{ route('admin.transport.alerts.export',request()->query()) }}" class="h-fit rounded-lg border border-cyan-700 px-4 py-2 text-cyan-800">Export CSV</a></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-5"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Trip code" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option>@foreach(['OPEN','ACKNOWLEDGED','RESOLVED'] as $state)<option>{{ $state }}</option>@endforeach</select><select name="severity" class="rounded-lg border-slate-300"><option value="">All severity</option><option>WARNING</option><option>CRITICAL</option></select><select name="type" class="rounded-lg border-slate-300"><option value="">All types</option>@foreach($types as $type)<option>{{ $type }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Alert</th><th>Trip</th><th>Severity / status</th><th>Measured / threshold</th><th>Detected</th></tr></thead><tbody>@forelse($alerts as $alert)<tr class="border-b"><td class="p-4"><a class="font-semibold text-cyan-700" href="{{ route('admin.transport.alerts.show',$alert) }}">{{ str_replace('_',' ',$alert->type) }}</a></td><td>{{ $alert->trip?->trip_code }}<div>{{ $alert->trip?->organization?->name }}</div></td><td class="{{ $alert->severity==='CRITICAL'?'text-red-700':'' }}">{{ $alert->severity }} · {{ $alert->status }}</td><td>{{ $alert->measured_value ?? '—' }} / {{ $alert->threshold_value ?? '—' }}</td><td>{{ $alert->last_detected_at?->format('Y-m-d H:i:s') }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center">No alerts match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $alerts->links() }}</div></section></x-layouts.admin>
