<x-layouts.admin title="Telemetry | FishTrace">@include('admin.transport._nav')<header class="flex justify-between gap-4"><div><p class="text-sm font-medium text-cyan-700">Permanent MySQL history</p><h1 class="text-3xl font-bold">Telemetry</h1><p class="text-slate-500">Raw payloads and coordinates are intentionally excluded.</p></div><a href="{{ route('admin.transport.telemetry.export',request()->query()) }}" class="h-fit rounded-lg border border-cyan-700 px-4 py-2 text-cyan-800">Export CSV</a></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Device or trip code" class="rounded-lg border-slate-300"><select name="device_id" class="rounded-lg border-slate-300"><option value="">All devices</option>@foreach($devices as $device)<option value="{{ $device->id }}">{{ $device->device_code }}</option>@endforeach</select><select name="trip_id" class="rounded-lg border-slate-300"><option value="">All trips</option>@foreach($trips as $trip)<option value="{{ $trip->id }}">{{ $trip->trip_code }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Recorded</th><th>Trip / device</th><th>Product / air</th><th>Humidity</th><th>Speed</th><th>Battery / signal</th><th>Door</th></tr></thead><tbody>@forelse($readings as $reading)<tr class="border-b"><td class="p-4">{{ $reading->recorded_at?->format('Y-m-d H:i:s') }}</td><td><a class="text-cyan-700" href="{{ route('admin.transport.trips.show',$reading->trip) }}">{{ $reading->trip?->trip_code }}</a><div>{{ $reading->device?->device_code }}</div></td><td>{{ $reading->product_temperature }} / {{ $reading->air_temperature }} °C</td><td>{{ $reading->humidity }}%</td><td>{{ $reading->speed_kph ?? '—' }}</td><td>{{ $reading->battery_percentage }}% / {{ $reading->signal_strength }}</td><td>{{ $reading->door_open?'OPEN':'CLOSED' }}</td></tr>@empty<tr><td colspan="7" class="p-10 text-center">No readings match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $readings->links() }}</div></section></x-layouts.admin>
