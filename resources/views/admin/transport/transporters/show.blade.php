<x-layouts.admin title="{{ $transporter->name }} | FishTrace">@include('admin.transport._nav')<a href="{{ route('admin.transport.transporters.index') }}" class="text-sm text-cyan-700">← Transporters</a><h1 class="mt-2 text-3xl font-bold">{{ $transporter->name }}</h1><p class="text-slate-500">{{ $transporter->email }} · {{ $transporter->organizations->pluck('name')->join(', ') }}</p><section class="mt-6 grid gap-4 md:grid-cols-3"><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-slate-500">Status</p><p class="mt-2 text-xl font-bold">{{ $transporter->status }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-slate-500">Created trips</p><p class="mt-2 text-xl font-bold">{{ $transporter->transport_trips_count }}</p></article><article class="rounded-xl bg-white p-5 shadow-sm"><p class="text-slate-500">Last login</p><p class="mt-2 font-bold">{{ $transporter->last_login_at?->format('Y-m-d H:i') ?? 'Never' }}</p></article></section><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Newest 100 trips</h2></header><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Trip</th><th>Vehicle</th><th>Status</th><th>Batches</th><th>Readings / alerts</th></tr></thead><tbody>@foreach($transporter->transportTrips as $trip)<tr class="border-b"><td class="p-4"><a class="text-cyan-700" href="{{ route('admin.transport.trips.show',$trip) }}">{{ $trip->trip_code }}</a></td><td>{{ $trip->vehicle?->registration_number }}</td><td>{{ $trip->status->value }}</td><td>{{ $trip->batches_count }}</td><td>{{ $trip->readings_count }} / {{ $trip->alerts_count }}</td></tr>@endforeach</tbody></table></section></x-layouts.admin>
