<x-layouts.admin title="Transporters | FishTrace">@include('admin.transport._nav')<header><p class="text-sm font-medium text-cyan-700">Transport & IoT</p><h1 class="text-3xl font-bold">Transporters</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Name or email" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $organization)<option value="{{ $organization->id }}" @selected(($filters['organization_id'] ?? '')===$organization->id)>{{ $organization->name }}</option>@endforeach</select><select name="status" class="rounded-lg border-slate-300"><option value="">All statuses</option><option>ACTIVE</option><option>DISABLED</option></select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b bg-slate-50 text-slate-500"><tr><th class="p-4">Transporter</th><th>Organization</th><th>Created trips</th><th>Status</th></tr></thead><tbody>@forelse($transporters as $transporter)<tr class="border-b"><td class="p-4"><a class="font-semibold text-cyan-700" href="{{ route('admin.transport.transporters.show',$transporter) }}">{{ $transporter->name }}</a><div class="text-slate-500">{{ $transporter->email }}</div></td><td>{{ $transporter->organizations->first()?->name }}</td><td>{{ $transporter->transport_trips_count }}</td><td>{{ $transporter->status }}</td></tr>@empty<tr><td colspan="4" class="p-10 text-center text-slate-500">No transporters match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $transporters->links() }}</div></section></x-layouts.admin>
