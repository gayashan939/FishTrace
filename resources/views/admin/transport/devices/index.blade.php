<x-layouts.admin title="IoT Devices | FishTrace">@include('admin.transport._nav')<header><p class="text-sm font-medium text-cyan-700">Transport & IoT</p><h1 class="text-3xl font-bold">IoT devices</h1></header><form class="mt-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Code, name, or serial" class="rounded-lg border-slate-300"><select name="organization_id" class="rounded-lg border-slate-300"><option value="">All organizations</option>@foreach($organizations as $organization)<option value="{{ $organization->id }}">{{ $organization->name }}</option>@endforeach</select><input name="status" value="{{ $filters['status'] ?? '' }}" placeholder="Status" class="rounded-lg border-slate-300"><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Device</th><th>Status</th><th>Latest assignment</th><th>Last seen</th><th>Health</th><th>Sync</th></tr></thead><tbody>@forelse($devices as $device)<tr class="border-b"><td class="p-4"><a class="font-semibold text-cyan-700" href="{{ route('admin.transport.devices.show',$device) }}">{{ $device->display_name }}</a><div>{{ $device->device_code }} · {{ $device->serial_number }}</div></td><td>{{ $device->status }}</td><td>{{ $device->assignments->first()?->trip?->trip_code ?? 'Unassigned' }}</td><td>{{ $device->last_seen_at?->format('Y-m-d H:i') ?? 'Never' }}</td><td>{{ $device->battery_percentage ?? '—' }}% · {{ $device->signal_strength ?? '—' }} dBm</td><td>{{ $device->syncCursor?->last_successful_sync_at?->format('Y-m-d H:i') ?? 'Never' }}<div class="text-red-700">{{ $device->unresolved_failures_count }} unresolved</div></td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No devices match.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $devices->links() }}</div></section></x-layouts.admin>
