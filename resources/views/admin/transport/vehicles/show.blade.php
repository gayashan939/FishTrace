<x-layouts.admin title="{{ $vehicle->name }} | FishTrace">@include('admin.transport._nav')<a href="{{ route('admin.transport.vehicles.index') }}" class="text-sm text-cyan-700">← Vehicles</a><h1 class="mt-2 text-3xl font-bold">{{ $vehicle->name }}</h1><p class="text-slate-500">{{ $vehicle->registration_number }} · {{ $vehicle->organization?->name }} · {{ $vehicle->is_active?'ACTIVE':'INACTIVE' }}</p><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Newest 100 trips ({{ $vehicle->trips_count }} total)</h2></header><table class="w-full text-left text-sm"><thead class="border-b"><tr><th class="p-4">Trip</th><th>Creator</th><th>Route</th><th>Status</th><th>Operational records</th></tr></thead><tbody>@foreach($vehicle->trips as $trip)<tr class="border-b"><td class="p-4"><a class="text-cyan-700" href="{{ route('admin.transport.trips.show',$trip) }}">{{ $trip->trip_code }}</a></td><td>{{ $trip->creator?->name }}</td><td>{{ $trip->origin }} → {{ $trip->destination }}</td><td>{{ $trip->status->value }}</td><td>{{ $trip->batches_count }} batches · {{ $trip->readings_count }} readings · {{ $trip->alerts_count }} alerts</td></tr>@endforeach</tbody></table></section></x-layouts.admin>
