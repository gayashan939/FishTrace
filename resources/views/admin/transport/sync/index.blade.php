<x-layouts.admin title="Firebase Sync Health | FishTrace">@include('admin.transport._nav')<header><p class="text-sm font-medium text-cyan-700">Firebase import observability</p><h1 class="text-3xl font-bold">Sync health</h1><p class="text-slate-500">Credentials, Firebase UIDs, and failed payloads are excluded.</p></header><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Device cursors</h2></header><table class="w-full text-left text-sm"><thead><tr><th class="p-4">Device</th><th>Last success</th><th>Last message</th><th>Last error</th><th>Unresolved failures</th></tr></thead><tbody>@foreach($devices as $device)<tr class="border-t"><td class="p-4"><a class="text-cyan-700" href="{{ route('admin.transport.devices.show',$device) }}">{{ $device->device_code }}</a></td><td>{{ $device->syncCursor?->last_successful_sync_at?->format('Y-m-d H:i:s') ?? 'Never' }}</td><td>{{ $device->syncCursor?->last_message_id ?? '—' }}</td><td class="text-red-700">{{ $device->syncCursor?->last_error ?? '—' }}</td><td>{{ $device->unresolved_failures_count }}</td></tr>@endforeach</tbody></table><div class="p-4">{{ $devices->links() }}</div></section><section class="mt-6 overflow-hidden rounded-xl bg-white shadow-sm"><header class="border-b p-5"><h2 class="font-bold">Failure ledger</h2></header><table class="w-full text-left text-sm"><thead><tr><th class="p-4">Device</th><th>Message</th><th>Error</th><th>Retries</th><th>Last failed</th><th>Status</th></tr></thead><tbody>@forelse($failures as $failure)<tr class="border-t"><td class="p-4">{{ $failure->device?->device_code ?? 'Unknown' }}</td><td>{{ $failure->message_id }}</td><td>{{ $failure->error_code }} · {{ $failure->error_message }}</td><td>{{ $failure->retry_count }}</td><td>{{ $failure->last_failed_at?->format('Y-m-d H:i:s') }}</td><td>{{ $failure->resolved_at?'RESOLVED':'UNRESOLVED' }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No sync failures.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $failures->links() }}</div></section></x-layouts.admin>
